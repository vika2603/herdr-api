/bin/
/dist/
*.test
*.out
coverage.*
.DS_Store

# Binaries a `go build ./...` or `go build ./examples/...` leaves behind, named
# after the package directory they came from.
/herdr-apigen
/agent-status
/agent-board

# Binaries the [[build]] command of each example manifest produces in place.
# Listed one by one: a herdr-* glob here would also cover herdr-plugin.toml.
/examples/agent-status/herdr-agent-status
/examples/agent-board/herdr-agent-board
/examples/worktree-bootstrap/herdr-worktree-bootstrap
