version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - errorlint
    - gocritic
    - misspell
    - revive
    - unconvert
  exclusions:
    generated: lax
    paths:
      - internal/e2e

formatters:
  enable:
    - gofmt
    - goimports
