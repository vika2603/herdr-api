// Command herdr-apigen generates the Go API surface of package herdr from
// the Herdr socket API schema. See docs/design.md.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "herdr-apigen: not implemented")
	os.Exit(1)
}
